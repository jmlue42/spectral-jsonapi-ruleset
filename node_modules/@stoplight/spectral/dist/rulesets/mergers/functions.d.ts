import { RuleCollection } from '../../types';
import { RulesetFunctionCollection } from '../../types/ruleset';
export declare function mergeFunctions(target: RulesetFunctionCollection, source: RulesetFunctionCollection, rules: RuleCollection): void;
