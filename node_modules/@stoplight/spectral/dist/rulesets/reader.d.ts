import { IHttpAndFileResolverOptions } from '../resolvers/http-and-file';
import { IRuleset } from '../types/ruleset';
export interface IRulesetReadOptions extends IHttpAndFileResolverOptions {
    timeout?: number;
}
export declare function readRuleset(uris: string | string[], opts?: IRulesetReadOptions): Promise<IRuleset>;
