import { IRuleResult } from '../../../spectral';
import { ILintConfig } from '../../../types/config';
export declare function lint(documents: Array<number | string>, flags: ILintConfig): Promise<IRuleResult[]>;
