import { RuleCollection } from '../../../../types';
import { ILintConfig } from '../../../../types/config';
export declare const skipRules: (rules: RuleCollection, flags: ILintConfig) => RuleCollection;
