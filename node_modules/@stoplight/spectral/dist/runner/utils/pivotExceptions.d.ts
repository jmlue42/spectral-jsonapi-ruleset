import { Dictionary, JsonPath } from '@stoplight/types';
import { RunRuleCollection } from '../../types';
import { RulesetExceptionCollection } from '../../types/ruleset';
export declare type ExceptionLocation = {
    source: string;
    path: JsonPath;
} | {
    source: string;
    path: null;
} | {
    source: null;
    path: JsonPath;
};
export declare const pivotExceptions: (exceptions: RulesetExceptionCollection, runRules: RunRuleCollection) => Dictionary<ExceptionLocation[], string>;
